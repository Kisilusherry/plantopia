// Main page functionality
document.addEventListener('DOMContentLoaded', function() {
    const PLANTS_URL = 'https://perenual.com/api/species-list?key=sk-u1HU65db614cf00c44355';

    function fetchPlantListings() {
        // Fetch plant listings from the server
        // Implement logic to fetch plant listings
    }

    // Add logic to render plant listings
    // ...

    // Fetch plant listings when main page is loaded
    if (window.location.pathname === '/main.html') {
        fetchPlantListings();
    }
});

